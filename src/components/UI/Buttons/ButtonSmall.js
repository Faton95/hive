import styled from 'styled-components'
import { Button } from 'ui-cubic'

export default styled(Button)`
  border-radius: 6px;
  font-size: 14px;
  font-weight: normal;
  height: 36px;
  padding: 0 20px;
`
